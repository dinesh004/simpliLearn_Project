// Task-4 Create an array of strings and print the strings that have a length of 5 to the console.
var strgarr = ["Dinesh", "Dhiraj", "Rahul", "Excel", "Level"];
for (var cntr = 0; cntr < strgarr.length; cntr++) {
    if (strgarr[cntr].length == 5) {
        console.log(strgarr[cntr]);
    }
}
